# Introduction

#參考 zaoa3345678-arch

## git and vs code
- installing git
- using 'git clone repository.git' to hard copy github repository
- remove .git folder (hidden in the repository)
- git config --global user.name 'name'
- git config --global user.email 'email'
- git init
- git add .
- cd .. 回到上一個資料夾
- git commit -m 'message'
- git checkout -b Branch_411252648
- git switch master
- git switch Branch_411252648

# LFS
 - 下載 git large file storage
 - git lfs install
 - git lfs track *.exe 

# github and git
- create new repository in github
- git remote add origin yours.git
- git branch -M main
- git push -u origin main
- check status of the new github repository
# tkinter
- python ./檔案名稱.py
  
